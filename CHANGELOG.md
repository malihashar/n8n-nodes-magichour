# Changelog

## 0.2.0

- Cover all 27 Magic Hour generation endpoints (parity with Apify Actors).
- Add Audio resource (Voice Generator, Voice Cloner).
- Align output fields with the Apify dataset contract (`externalId`, `outputUrl`, `creditsCharged`, …).
- Add shared catalog (`catalog/operations.json`) and `npm run coverage`.
- Add example workflow `workflows/image-to-video.example.json`.

## 0.1.0

- Initial Magic Hour community node (9 video/image operations).
